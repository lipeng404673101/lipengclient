using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonEnabled {
    /*
    private UIButton but;

    public void AAAbtn()
    {
        but = GetComponent<UIButton>();
        but.state = UIButton.State.Disabled;
        // 如果不取消掉碰撞器，那么鼠标滑过按钮的时候还会变成hover状态  
        GetComponent<BoxCollider>().enabled = false;  
    }
     * */
}
